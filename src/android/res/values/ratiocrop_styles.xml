<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Keeps CropImageActivity's toolbar below the status bar instead of drawing edge-to-edge -->
    <style name="RatioCropTheme" parent="@android:style/Theme.Holo.Light">
        <item name="android:windowTranslucentStatus">false</item>
        <item name="android:fitsSystemWindows">true</item>
    </style>
</resources>
